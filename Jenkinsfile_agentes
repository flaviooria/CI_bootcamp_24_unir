pipeline {
    agent none

    stages {
        stage('Use agent ssh') {
            agent { label 'dev'}

            steps {
                sh 'whoami'
                sh 'hostname -i'
                sh 'echo $WORKSPACE'
            }
        }
    }

}